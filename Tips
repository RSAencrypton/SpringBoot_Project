redis分片集群：
1.散列插槽
 a. 为什么分片数据要使用散列插槽
    方便redis进行数据转移或者扩容，数据不会因节点的意外宕机而丢失
    插槽的计算规则如下：
    key中包含{}且{}中一定包含至少一个字符，且{}中的字符不能是空格。{}中的内容为有效字符进行计算
    key中不包含{}则整个key认为是有效部分
    slot = CRC16(key) % 16384
2.故障转移（当一个实例与其他实例失去连接，redis会自动升级丢失实例下的子节点为主节点，
            即使节点重新连接，他也会被降级为子节点）